# ==============================================================================
# AssistantAudit v2 — Backend Dependencies (compat shim, TOS-102 / AC-4)
# ==============================================================================
# Ce fichier est conservé pour compatibilité historique et installe l'ensemble
# (runtime + dev + prod). Pour des installs ciblées, utiliser :
#   pip install -r requirements/base.txt   # runtime seul
#   pip install -r requirements/dev.txt    # base + tests + lint
#   pip install -r requirements/prod.txt   # base + monitoring (Sentry, Prometheus)
-r requirements/dev.txt
-r requirements/prod.txt
